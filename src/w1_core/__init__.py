"""w1_core — the portable, platform-agnostic dictation engine.

Contains zero desktop / ML / audio-device imports (enforced by import-linter and a
core-only CI install). This is the layer that survives unchanged to iOS / Android.
"""

__version__ = "1.1.0"
